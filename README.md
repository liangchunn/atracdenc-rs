# atracdenc Rust port

This repository is a Rust port of `atracdenc`. The original C++ reference
implementation is kept in `./atracdenc/` and remains the source of truth while
the Rust workspace is built out in phases. The port is distributed under
LGPL-2.1-or-later.
